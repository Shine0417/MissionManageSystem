# .travis.yml

language: ruby
rvm:
- 3.0.1
install:
  - bundle install
services:
  - postgresql
script:
  - RAILS_ENV=test bundle exec rails webpacker:compile
  - bundle exec rails db:migrate:reset RAILS_ENV=test
  - bundle exec rspec