<%= @mission.title%>
<%= @mission.description%>
<%= mission_due_date(@mission.due_date) %>
<%= render "status_form", mission: @mission, redirect: :show %>
<%= link_to t('edit') , edit_mission_path(@mission)%>
<%= link_to t('home') ,missions_path%>