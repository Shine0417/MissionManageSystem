<%= form_for mission do |f|%>
  <%= hidden_field_tag :redirect, true %>
  <%= f.select :status, options_for_select(Mission.statuses.keys.map{|stat| [t("mission.statuses.#{stat}"), stat] }, mission.status), {}, :onchange => "this.form.submit()" %>
<% end %>
