<%= form_for mission do |f|%>
  <%= hidden_field_tag :redirect, redirect %>
  <%= f.select :status, options_for_select(translate(Mission.statuses.keys, Mission), mission.status), {}, :onchange => "this.form.submit()", class: "btn btn-outline-secondary dropdown-toggle" %>
<% end %>
